# Fate/Grand Automata

FGOの周回バトルを自動化するAndroidアプリ。画面をテンプレートマッチングで読み取り、スキル・カード・宝具を自律的に選択して戦闘を進める。

## Language

### カード選択(AutoBattle戦闘ターン)

**カードの優先順位(Card Priority)**:
種別(Buster/Arts/Quick)×属性(弱点/耐性/通常)の組み合わせごとに、手札全体をグローバルにランキングする既存の優先度指定(`CardPriority`)。上位のカードから3枚が選ばれる。「同種別のカードが並ぶ」選択にしかならない。
_Avoid_: カード優先度DSL単体を指すときに「型パターン」と混同しないこと。

**ブレイブチェイン(Brave Chain)**:
同じサーヴァントのカードを連続して使うことで得られる演出・ボーナス。既存の`BraveChainEnum`(None/WithNP/Avoid)で、狙う/避けるを設定できる。
_Avoid_: コンボ ―― プレイヤー俗称としては使われるが、本プロジェクトのコードでは「ブレイブチェイン」を正式名称とし、「コンボ」は新設の「型パターン」と紛らわしいため使わない。

**型パターン(Type Pattern)**:
最終的に選ぶ3枚のカードについて、ポジションごとに要求する種別(B/A/Q)を順序付きで指定する新しい優先度レイヤー(例: `"BAB"`)。既存の「カードの優先順位」がカード全体をランキングするのに対し、型パターンは「1枚目はB、2枚目はA、3枚目はB」のように位置ごとに異なる型を明示できる。手札がその並びを満たせない場合は不成立となり、次候補または既存の「カードの優先順位」にフォールバックする。
_Avoid_: コンボ、CardCombo ―― 「ブレイブチェイン」の俗称と衝突するため不採用。
