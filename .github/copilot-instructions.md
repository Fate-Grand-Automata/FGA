# Copilot instructions

The repo-wide guidance for this project lives in [`AGENTS.md`](../AGENTS.md) — read it first.
Path-specific guidance lives in [`docs/agents/`](../docs/agents/); the files in
[`.github/instructions/`](instructions/) point at it with an `applyTo` glob.
