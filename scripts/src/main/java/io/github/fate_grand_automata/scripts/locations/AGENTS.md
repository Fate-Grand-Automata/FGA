# AGENTS.md — locations/

Every coordinate here is expressed at **1440p** while image matching runs at **720p**, and the
X origin depends on the device aspect ratio. Read `docs/agents/coordinates.md` (from the repo
root) before adding or changing any value; `CONTRIBUTING.md` covers measuring new coordinates
from a screenshot.
